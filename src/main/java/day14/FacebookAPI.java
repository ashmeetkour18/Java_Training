/*
package day14;

import org.springframework.web.bind.annotation.*;

import java.util.Arrays;
import java.util.List;
import java.util.Map;

@RestController
public class FacebookAPI {

        */
/* @GetMapping("/t")
         List<String> getTweets() {
             return Arrays.asList("tweet-1", "tweet-2", "tweet-3");
         }*//*

 */
/*@GetMapping("/tweets.mustache")
   List<String> getTweets(@RequestParam String mention, @RequestParam String word) {
       return Arrays.asList(mention,word);
   }*//*


    @PostMapping("/register")
    String register(@RequestBody String body) {
        return "registration successful";
    }
    @PostMapping("/follow")
    List<String> follow(@RequestBody String body) {
        return Arrays.asList(body);
    }
    @PostMapping("/friend")
    List<String> friend(@RequestBody String body) {
        return Arrays.asList(body);
    }
    @PostMapping("/unFriend")
    List<String> unFriend(@RequestBody String body) {
        return Arrays.asList(body);
    }
    @PostMapping("/post")
    List<String> post(@RequestBody String body) {
        return Arrays.asList(body);
    }
    @GetMapping("/likes")
    String getLikes(@RequestParam Map<String,String> requestParams) {
        String count="100";
        return count;
    }
    }*/
